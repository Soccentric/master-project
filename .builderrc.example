# ==============================================================================
# Builder Configuration File
# ==============================================================================
# Copy this file to .builderrc and customize as needed
# These settings override Makefile defaults
#
# Usage:
#   cp .builderrc.example .builderrc
#   # Edit .builderrc with your preferences
#   make build raspberry-pi raspberrypi5 core-image-base
#
# ==============================================================================

# Build Variant
# Options: debug, release, production
# debug      - Debug symbols, extra logging, development tools
# release    - Optimized, balanced (default)
# production - Hardened security, read-only rootfs
BUILD_VARIANT=release

# BitBake Parallel Tasks
# Number of BitBake tasks to run in parallel
# Recommendation: CPU cores - 2 (leave room for system)
# Default: nproc (all available cores)
BB_NUMBER_THREADS=8

# Make Parallel Jobs
# Number of make jobs to run in parallel during compilation
# Recommendation: CPU cores or CPU cores + 2
# Default: -j nproc
PARALLEL_MAKE=-j 8

# Remove Work Files After Build
# 1 = Remove work files (saves disk space, recommended)
# 0 = Keep work files (faster rebuilds, uses more space)
RM_WORK=1

# Docker Resource Limits (optional)
# Uncomment to limit Docker container resources
# DOCKER_MEMORY=16g
# DOCKER_CPUS=8

# Proxy Settings (optional)
# Uncomment and configure if behind a corporate proxy
# HTTP_PROXY=http://proxy.example.com:3128
# HTTPS_PROXY=http://proxy.example.com:3128
# FTP_PROXY=http://proxy.example.com:3128
# NO_PROXY=localhost,127.0.0.1,.example.com

# Remote SSTATE Mirror (optional)
# Uncomment to use remote sstate cache mirror
# SSTATE_MIRRORS=file://.* http://sstate-cache.example.com/PATH

# Download Mirror (optional)
# Uncomment to use download mirror for source packages
# SOURCE_MIRROR_URL=http://downloads.example.com/

# Advanced: Custom KAS Options (optional)
# Pass additional options to kas command
# KAS_OPTIONS=--update

# Advanced: BitBake Extra Options (optional)
# Additional BitBake configuration
# INHERIT += "buildhistory"
# BUILDHISTORY_COMMIT = "1"
